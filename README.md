# OP-Etch-a-sketch
Etch-a-Sketch is a browser-based drawing app built with HTML, CSS (Flexbox), and JavaScript. It generates a dynamic grid where users draw by hovering over squares, practicing DOM manipulation, event handling, and interactive UI logic.
